= form_with model: @data_source do |f|
  - if @data_source.errors.any?
    #error_explanation
      %h2= "#{pluralize(@data_source.errors.count, "error")} prohibited this data_source from being saved:"
      %ul
        - @data_source.errors.full_messages.each do |message|
          %li= message

  .form-group
    = f.text_field :name, autofocus: true, class: 'form-control form-control-lg', placeholder: 'Name'
  .form-group
    = f.text_field :base_url, class: 'form-control form-control-lg', placeholder: 'Base URL (optional)'
  .form-group
    = f.text_area :description, rows: 15, class: 'form-control', placeholder: 'Description'
    %small.form-text.text-muted You can write Markdown in the description.
  .form-group
    = f.submit 'Save', class: 'btn btn-lg btn-light'
