%h1.display-4 Users

- if @users.length > 0
  %table.table.table-striped
    %tbody
      - @users.each do |user|
        %tr
          %td.td-link{width: 10}
            = link_to user.name, user
- else
  No users found...
